#!/usr/bin/env bash
# Point MNEXUS_APKTOOL_PATH at this file. Mirrors the real apktool CLI surface
# (`--version`, `d -f -o <dir> <apk>`) — the apktool_engine never knows.
set -Eeuo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/_docker_tool.sh" apktool "$@"
