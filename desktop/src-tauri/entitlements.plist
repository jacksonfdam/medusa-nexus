<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Frida injects code at runtime via the bundled mnexus sidecar. Under the
    hardened runtime (required for notarization) injection dies unless we allow
    unsigned executable memory and disable library validation. These are the
    minimum entitlements for a Frida-driven analysis tool — scoped to this app,
    not the system.
  -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
</dict>
</plist>
